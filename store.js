import { atom } from 'jotai';

export const favouritesAtom = atom();      // undefined until hydrated by RouteGuard
export const searchHistoryAtom = atom();   // undefined until hydrated by RouteGuard
